import React from 'react'


const TabOption = ({activeTab, setActiveTab}) => {
  return (
    <div> 
        this is tab option
        {/* <button onClick={() => setActiveTab("Delivery")}>onClick</button> */}
        {/* <button onClick={() => setActiveTab("Nightlife")}>ON click</button> */}
        {/* <button onClick={() => setActiveTab("DiningOut")}>onClick</button> */}
    </div>
  );
};

export default TabOption;